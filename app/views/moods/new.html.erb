<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f5f5f5; /* Set a light background color */
      color: #333; /* Set the text color */
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    h1 {
      font-size: 2.5em;
      color: #007bff; /* Set a primary color for headings */
      margin-bottom: 20px;
    }

    form  {
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-bottom: 5px;
      color: #555; /* Set label color */
    }

    input[type="text"],
    textarea,
    input[type="date"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 10px;
      border: 1px solid #ddd; /* Add a border to form elements */
      border-radius: 5px;
      box-sizing: border-box;
    }

    button[type="submit"] {
      background-color: #007bff; /* Set a primary color for the submit button */
      color: #fff; /* Set text color for the submit button */
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button[type="submit"]:hover {
      background-color: #0056b3; /* Darken the background on hover for the submit button */
    }

    .field {
      margin-bottom: 20px;
    }

    .actions {
      margin-top: 20px;
    }

    a.back-link {
      color: #007bff; /* Set a link color for the back link */
      text-decoration: none;
    }

    a.back-link:hover {
      text-decoration: underline; /* Add underline on hover for the back link */
    }
  </style>
</head>
<body>
  <h1>Create a New Mood</h1>


  <%= form_with(model: @mood, local: true) do |form| %>
    <div class="field">
      <%= form.label :phrase %>
      <%= form.text_field :phrase, required: true %>
    </div>


    <div class="field">
      <%= form.label :dateSearched %>
      <%= form.date_field :dateSearched, required: true %>
    </div>

    <div>
      <% form.label :user_id, style: "display: block" %>
      <%= form.text_field :user_id, value:current_user.id, type: :hidden %>
    </div>

    <div class="actions">
      <%= form.submit 'Create Mood' %>
    </div>
  <% end %>

  <%= link_to 'Back', moods_path, class: 'back-link' %>
</body>
</html>

