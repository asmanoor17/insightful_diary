<h1><em> Insightful Diary </em></h1>
<% if user_signed_in?%>
  <div> 👋🏻 
    Welcome, <%= current_user.id%>! </div>
  <%=button_to "Sign out", destroy_user_session_path, method: :delete, class:"btn btn-dark"%>

<% else %>
  <%= button_to "Sign in", new_user_session_path, class:"btn btn-dark"%>
<% end %>

